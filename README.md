## Aplicación de consola de comandos

Simple aplicación en Node con un CRUD para mantener una lista de tareas con su estado (completado o no).

Es necesario instalar los paquetes de Node

````
npm install
````
